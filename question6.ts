//store a person name whit capital and small
const personNameWithWhitespace: string = "\t\n  AyEsHa  \t\n";

//write name with surrounding whitespace
console.log(`Name with whitespace: "${personNameWithWitespace}"`);

//print the name with whitespace
const strippedName: string = personNameWithWitespace.trim();
console.log(`Stripped name: "${strippedName}"`);