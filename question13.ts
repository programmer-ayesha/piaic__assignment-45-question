//favorite modes of transport (aeroplane brands)
const transportationModes: string[] = ["qantas", "qatar", "etihad"];
//print statements
for ( const mode of transportationModes) {
    console.log(`i would like to own a ${mode}.`);
}
